#include "main.h"

#define nXY(x,y) { note.posX = x; note.posY = y; }
#define nX note.posX
#define nY note.posY
#define nL note.isLive


typedef struct _Note 
{
	int posX, posY;
	bool isLive;
}Note;

Note note;

void CreateNote(int b) 
{
	if (b == 0)
	{
	}

	else if (b == 1)
	{

	}

	else if (b == 2)
	{

	}
}

void PrintNote()
{

}
